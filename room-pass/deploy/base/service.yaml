apiVersion: v1
kind: Service
metadata:
  name: room-pass
  labels:
    app: room-pass
spec:
  selector:
    app: room-pass
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: metrics
      port: 9090
      targetPort: metrics
