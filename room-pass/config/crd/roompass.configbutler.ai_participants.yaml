---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: participants.roompass.configbutler.ai
spec:
  group: roompass.configbutler.ai
  names:
    kind: Participant
    listKind: ParticipantList
    plural: participants
    singular: participant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.roomRef.name
      name: Room
      type: string
    - jsonPath: .spec.revoked
      name: Revoked
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              displayName:
                description: DisplayName is an unverified demo label, never an authorization
                  key.
                maxLength: 64
                minLength: 1
                pattern: ^[^<>\x00-\x1f\x7f]+$
                type: string
                x-kubernetes-validations:
                - message: displayName is immutable
                  rule: self == oldSelf
              revoked:
                default: false
                type: boolean
                x-kubernetes-validations:
                - message: revocation is irreversible
                  rule: '!oldSelf || self'
              roomRef:
                properties:
                  name:
                    maxLength: 253
                    minLength: 1
                    type: string
                  uid:
                    description: UID prevents name reuse from reviving enrollment.
                    maxLength: 128
                    minLength: 1
                    type: string
                required:
                - name
                - uid
                type: object
                x-kubernetes-validations:
                - message: roomRef is immutable
                  rule: self == oldSelf
            required:
            - displayName
            - revoked
            - roomRef
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
