voter/bin/
voter/voter
external-code
external/
# Local devcontainer environment (GH_TOKEN etc). Never commit.
.env

# Task's fingerprint cache (task --status / sources: checksums).
.task/
*.ignore.*
# Theme screenshots from `task frontend:theme` — look at them, do not commit them.
frontend/theme-shots/
