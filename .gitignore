voter/bin/
voter/voter
k8s-examples/zot-pull.secret.example.yaml
external-code
external/
# Local devcontainer environment (GH_TOKEN etc). Never commit.
.env

# Task's fingerprint cache (task --status / sources: checksums).
.task/
